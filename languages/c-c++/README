# C & C++

I recommed using `clang-format` to format C and C++ code. 
This is a formatter that comes installed with VSCode's C/C++ extension, and can be installed easily if you are using a different text editor.